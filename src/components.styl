.button
  
  padding 1em 1.3em
  background var(--blue-offset)
  border: 2px solid var(--blue-offset)
  color var(--blue)
  border-radius 5px
  transition: all 0.25s ease
  
  &:hover
    background var(--blue)
    color white
    border-color: white
